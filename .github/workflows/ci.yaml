name: Java CI - Enterprise Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - develop

permissions:
  checks: write
  contents: read

jobs:
  push-artifact-to-nexus:
    name: Push Artifact to Nexus
    runs-on: ubuntu-latest
    # needs: docker-build-and-scan


    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set dynamic version (based on commit SHA)
        run: mvn versions:set -DnewVersion=1.0.0-${{ github.sha }}

      - name: 🛠️ Build Artifact (JAR)
        run: mvn clean package

      - name: Deploy to Nexus
        run: |
          mvn deploy \
            -DaltDeploymentRepository=nexus::default::http://98.70.35.194:8081/repository/maven-snapshots/ \
            -Dnexus.username=admin \
            -Dnexus.password=admin
