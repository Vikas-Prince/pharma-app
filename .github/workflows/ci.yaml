name: Java CI - Enterprise Workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - develop

permissions:
  checks: write
  contents: read

jobs:
  slack-test:
    runs-on: ubuntu-latest

    steps:
      - name: ✅ Send basic Slack message
        run: |
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text":"📢 Hello from GitHub Actions! This is a test notification from *csi-pharma* pipeline."}' \
          ${{ secrets.SLACK_WEBHOOK_URL }}
